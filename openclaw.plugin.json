{
  "id": "bd-identity",
  "name": "Bead Identity & Project Management",
  "version": "3.0.0",
  "description": "Session-aware identity bead management (agent_self) with per-agent workspace memory + identity-protected project bead access (bd_project). Uses gateway-injected session context for tamper-proof identity resolution.",
  "entry": "index.ts",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
